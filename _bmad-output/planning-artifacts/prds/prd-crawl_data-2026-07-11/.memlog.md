---
topic: Objective primary-source Vietnam stock data crawler (volatility prediction)
updated: 2026-07-11T22:32
---

- (decision) Working mode = Fast path (draft from guide+brownfield, [ASSUMPTION] tags, iterate)
- (decision) Stakes = internal/research (PRD ~5-8 pages, moderate rigor)
- (decision) Scope v1 = crawl + metadata layer only (raw HTML/PDF + metadata schema, Tier-1 primary sources); NLP/feature/model = downstream separate PRD
- (assumption) Brownfield reuse: extend base_news_crawler.py / body_extractor / dedup / config / scheduler, not greenfield
- (decision) MVP Tier-1 priority = HOSE + HNX disclosures first (highest value, structured); UPCoM/VSDC/SSC/IR follow in later sprints
- (decision) Existing opinion-heavy crawlers (Vietstock analysis/SSI/HSC/VNDIRECT/Cafef) = keep as-is, separate; objective crawler is a new parallel layer, out of refactor scope
- (decision) Downstream consumer = VN30 Parkinson-volatility model (MACRO_FEATURES_PLAN.md, LSTM-GNN, horizons 1/5/10/22-day); NOT FinMarBa (which is US/global S&P500 sentiment, T+1 only, unrelated)
- (decision) Macro crawler (d671b57) already covers VNINDEX/DXY/USD-VND/SBV (Tier-1) = reuse; CPI/PMI/credit/FDI monthly = gap for later
- (change) Guide metadata schema (document_id,publish_time,crawl_time,company_code,company_name,title,raw_text,language,category,event_type,attachment_urls,checksum) is richer than current BaseNewsCrawler canonical schema (id,source,title,category,pub_date,url,author,lead,pdf_url,pdf_filename,collected_at,body) — PRD will extend schema with company_code/company_name/event_type/attachment_urls/checksum + UTC publish_time
- (decision) Source feasibility (empirically probed 2026-07-11): HNX+UPCoM = EASY HTTP (server-rendered+AJAX, sequential IDs, multi-year backfill); VSDC = EASY HTTP (ASP.NET /vi/ad/{id} sweep 50k-198k+); SSC = needs Playwright (Oracle ADF postback); HOSE = HARD (React SPA + OAuth-gated api.hsx.vn, ToS risk); Company IR = not worth (heterogeneous)
- (change) MVP recommendation adjusted: HNX+UPCoM + VSDC (easy HTTP, full backfill) instead of HOSE+HNX; HOSE deferred to later Playwright epic (ToS risk). Legal: Circular 96/2020 = public by law; Decree 13/2023 PDPD applies to personal data; avoid HOSE OAuth reverse-engineering
- (decision) VnExpress = Tier-2 news media (not in guide list, same class as Tuoi Tre/Thanh Nien); access EASY (RSS kinh-doanh/chung-khoan + google-news-sitemap, robots Allow); added as DEFERRED FR-15 — needs NLP layer to map company_code (news has no native ticker)
- (decision) MVP confirmed = HNX+UPCoM + VSDC; universe = VN30; HOSE = defer (FR-6 epic sau)
- (decision) Tier-2 news = 'lay het' full outlet list (VnExpress, VnEconomy, VietnamPlus, Bao Dau tu, Bao Chinh phu, TTXVN, Tuoi Tre, Thanh Nien, Nguoi Lao Dong, Kinh te Sai Gon); raw capture RSS-easy (v1, reuse BaseNewsCrawler); company_code/event_type enrichment = NLP-deferred
- (decision) Universe tightened to VN30-ONLY (30 HOSE large-caps). Implication: HNX/UPCoM demoted out of VN30 scope (wrong exchange — HNX-listed != HOSE/VN30); HOSE is the real VN30 disclosure source but hard; VSDC + Tier-2 news still apply VN30-filtered. Disclosure-source decision pending OQ-2
- (decision) OQ-2 resolved: VN30 disclosures via Tier-3 republish (Vietstock browser FR-16 + Cafef HTTP FR-17, reuse existing crawler infra, objective sections only per guide Tier-3 rule). HOSE-direct FR-6 dropped (OAuth/ToS). Final MVP = VN30 disclosures(Tier-3) + VSDC corporate-actions + Tier-2 news(10 bao) + schema/dedup/version/schedule
- (event) PRD finalized (status: final). Streamlined finalize at internal/research stakes: reviewer/polish passes skipped (finalize_reviewers empty); OQ-3 (macro gap), OQ-4 (event_type taxonomy), OQ-5 (PDPD redaction) deferred to architecture

# Test fixtures

Saved, network-free samples for smoke/functional tests:
- `*.html` — saved article pages (cafef/hsc/vndirect) for body_extractor tests
- `*.pdf` — sample PDFs (SSI/Vietstock) for extract_pdf_body tests

Do NOT hit live sites from tests — always read from here.
